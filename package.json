{
  "name": "tab-stash",
  "version": "0.1.0",
  "description": "Save selected Chrome tabs to named folders.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "happy-dom": "^14.0.0"
  }
}
